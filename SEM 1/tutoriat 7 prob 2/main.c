#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
bool rotire(char* x,char* y)
{
    int i;
    for(i=0;i<n;i++)
        for(i=0;i<n;i++)
    {
        if(x[i]==y[i])
    }
}

int main()
{ char x[6],y[6];
x='abcde';
y='abcde';
if(rotire(x,y)==true) printf("true");
else printf("else");
    return 0;
}
